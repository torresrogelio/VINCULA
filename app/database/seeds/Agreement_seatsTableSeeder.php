<?php

class Agreement_seatsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('agreement_seats')->truncate();

		$agreement_seats = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('agreement_seats')->insert($agreement_seats);
	}

}
