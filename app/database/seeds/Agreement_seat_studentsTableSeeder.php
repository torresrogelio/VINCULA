<?php

class Agreement_seat_studentsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('agreement_seat_students')->truncate();

		$agreement_seat_students = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('agreement_seat_students')->insert($agreement_seat_students);
	}

}
