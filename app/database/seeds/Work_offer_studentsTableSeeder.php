<?php

class Work_offer_studentsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('work_offer_students')->truncate();

		$work_offer_students = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('work_offer_students')->insert($work_offer_students);
	}

}
