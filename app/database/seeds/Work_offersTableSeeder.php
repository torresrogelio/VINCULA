<?php

class Work_offersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('work_offers')->truncate();

		$work_offers = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('work_offers')->insert($work_offers);
	}

}
