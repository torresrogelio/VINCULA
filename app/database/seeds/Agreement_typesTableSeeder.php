<?php

class Agreement_typesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('agreement_types')->truncate();

		$agreement_types = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('agreement_types')->insert($agreement_types);
	}

}
