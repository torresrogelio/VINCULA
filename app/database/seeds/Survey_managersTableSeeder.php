<?php

class Survey_managersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('survey_managers')->truncate();

		$survey_managers = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('survey_managers')->insert($survey_managers);
	}

}
