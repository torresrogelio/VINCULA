<?php

class Survey_studentsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('survey_students')->truncate();

		$survey_students = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('survey_students')->insert($survey_students);
	}

}
