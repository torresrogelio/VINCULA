<?php

class Exchange_subjectsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('exchange_subjects')->truncate();

		$exchange_subjects = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('exchange_subjects')->insert($exchange_subjects);
	}

}
